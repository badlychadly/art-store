import React from 'react';
import Loader from 'react-loader-spinner'

    const LoadingIcon = () => (
        <Loader 
        type="Bars"
        color="#ffffff"
        height="40"	
        width="20"
        />
    );

    export default LoadingIcon;